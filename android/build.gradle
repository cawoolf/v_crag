// Root build.gradle file (android/build.gradle)

buildscript {
    repositories {
        google()   // Ensure the google repository is added
        mavenCentral()  // Make sure Maven Central is added
    }
    dependencies {
        classpath("com.google.android.libraries.mapsplatform.secrets-gradle-plugin:secrets-gradle-plugin:2.0.1")
    }
}

allprojects {
    repositories {
        google()   // Ensure the google repository is added
        mavenCentral()  // Ensure Maven Central is added
    }
}





